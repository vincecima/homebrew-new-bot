[project]
name = "homebrew-new-bot"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Vince Cima", email = "contact@vincecima.com" }
]
dependencies = [
    "sqlite-utils>=3.36",
    "requests>=2.31.0",
]
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
ingest = 'homebrew_new_bot.ingest:ingest'
generate = 'homebrew_new_bot.generate:generate'

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "mypy>=1.10.0",
    "types-requests>=2.31.0.20240406",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/homebrew_new_bot"]
